# Zayra 🚀

A simple, lightweight, and easy-to-use WhatsApp automation library built on top of `whatsapp-web.js`.

## Installation

First, clone the repository and install dependencies:

```bash
npm install
